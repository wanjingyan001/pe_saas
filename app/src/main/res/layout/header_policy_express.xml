<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 社区 - 轮播图 -->
    <com.sogukj.pe.module.lpassistant.autoscroll.AutoScrollViewPager
        android:id="@+id/scroll_viewpager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <RelativeLayout
        android:id="@+id/rl_bottom"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignBottom="@id/scroll_viewpager"
        android:background="@drawable/bg_banner_bottom"
        android:gravity="bottom">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_centerVertical="true"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:singleLine="true"
            tools:text = "中国证券监督管理委员会行政许可实施程序规许可实施程序规"
            android:textColor="@color/white"
            android:textSize="15sp"
            />

    </RelativeLayout>
    <LinearLayout
        android:id="@+id/banner_ll_indicator"
        android:layout_width="match_parent"
        android:layout_height="@dimen/text_20"
        android:layout_above="@id/rl_bottom"
        android:gravity="center"
        android:orientation="horizontal">
    </LinearLayout>
</RelativeLayout>